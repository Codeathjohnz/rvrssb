PORT=5001
JWT_SECRET=replace_with_a_long_random_secret

# Only needed when the database is a separate service (Docker/Dokploy).
# Left unset, the app falls back to root/no-password on localhost for
# local development.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=ricedss
